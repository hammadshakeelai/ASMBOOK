{
  "name": "asmbook",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-2.0",
  "description": "ASMBOOK — a Jupyter-style notebook for learning 8086 assembly on a fully inspectable virtual machine",
  "scripts": {
    "test": "node tests/run.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@preact/signals": "^1.2.2",
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}